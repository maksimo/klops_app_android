<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/baseLayout"
    tools:context=".SplashActivity">

    <ImageView
        android:id="@+id/logo"
        android:layout_width="400dp"
        android:layout_height="400dp"
        android:background="@drawable/big_logo"
        android:layout_alignBottom="@+id/mercedes_logo"
        android:layout_alignParentLeft="false"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="170dp"
        android:layout_alignParentTop="true" />

    <ru.klops.klops.custom.TextViewProRegular
        android:id="@+id/support"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="400dp"
        android:text="@string/support"
        android:textColor="@color/darkGreyText"
        android:textSize="10sp" />

    <ImageView
        android:id="@+id/mercedes_logo"
        android:layout_width="140dp"
        android:layout_height="90dp"
        android:layout_below="@+id/support"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:background="@drawable/mercedes_logo" />
</RelativeLayout>